---
interface Props {
	id: string;
	title: string;
}

const { id, title} = Astro.props;
---

<article class="my-8">
	<header class="text-xl">
		<h1 id={id} class="text-2xl my-4">{title}</h1>
	</header>
	<section>
		<slot />
	</section>
</article>